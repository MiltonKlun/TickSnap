# Telegram Bot Token from BotFather
TELEGRAM_TOKEN=""

# Google Sheet ID (from the URL: .../d/SHEET_ID/edit...)
SHEET_ID=""

# Path to the Google Service Account JSON key file (relative to project root)
# Alternatively, you can use GOOGLE_CREDENTIALS_JSON_CONTENT to store the JSON content directly
GOOGLE_SERVICE_ACCOUNT_PATH="bot-credentials.json"

# Comma-separated list of Telegram User IDs allowed to use the bot
ALLOWED_USER_IDS=""

# Path to the directory containing .ttf font files (e.g., arial.ttf, arialbd.ttf)
# Use "." for the current directory if fonts are in the root, or "fonts/" if in a subdirectory.
FONT_PATH="."

# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL="INFO"
